
import 'package:flutter/material.dart';

class MyDekstop extends StatelessWidget {
  const MyDekstop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.deepPurple[300],
    );
  }
}
